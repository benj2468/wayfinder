[package]
name = "wayfinder-protos"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
prost = { version = "0.13" , default-features = false, features = ["derive"] }
# Optional: derive `serde::Serialize` on the generated message types so hosts
# (e.g. `wayfinderctl --output json`) can emit them directly.  Off by default so
# the no_std/embedded build pulls in no extra surface.
serde = { version = "1", optional = true, default-features = false, features = ["derive"] }

[features]
# Enable `#[derive(serde::Serialize)]` on every generated proto type.  Pulls in
# `serde/alloc` for the `Serialize` impls on `Vec<u8>`/`String` fields, since
# this crate is `no_std` + `alloc` (serde's `std`/`alloc` are off by default).
serde = ["dep:serde", "serde/alloc"]

[build-dependencies]
prost-build = "0.13"
